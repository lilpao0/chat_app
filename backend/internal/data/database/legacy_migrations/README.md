# Historical migration files

These files were preserved from `internal/migrate/` during the three-layer reorganization. They differ from the selected migrations and are not the active migration source. Use only `backend/migrations/` with the migrate CLI. Do not apply both sets.
